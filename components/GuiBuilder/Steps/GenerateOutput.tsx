/**
 * A step for generating the expected output of test cases in the `StdioTest` stage if this feature is enabled.
 */
function GenerateOutput() {
  return <div>GenerateOutputs</div>;
}

export default GenerateOutput;
