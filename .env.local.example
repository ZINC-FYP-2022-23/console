# Next.js server
PORT=3000
NEXT_PUBLIC_API_ENDPOINT=

# Input Files

# This is the path that stores the input files (i.e., submission, template, skeleton, provided files)
# of each assignment. It should share the same value with:
#  - `NEXT_PUBLIC_UPLOAD_DIR` in the "student-ui" repo's .env
#  - `context.inPaths.graderHostRoot` in grader's "config.properties"
NEXT_PUBLIC_UPLOAD_DIR=/path/to/shared/mount

# This specifies the directory name that stores the auto-generated expected outputs of test cases
# in the `StdioTest` pipeline stage. Those outputs will be stored to:
# `${NEXT_PUBLIC_UPLOAD_DIR}/assignment/<configId>/${GENERATED_EXPECTED_OUTPUT_FOLDER}`.
#
# It should share the same value with `context.generatedExpectedOutputDirectory` in grader's "config.properties".
# If that variable is not set in "config.properties", it defaults to `expectedOutput`.
GENERATED_EXPECTED_OUTPUT_FOLDER=expectedOutput

# OAuth (https://auth0.com/docs/quickstart/webapp/express)
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_BASE_URL=https://login.microsoftonline.com/common/v2.0
POST_LOGOUT_REDIRECT_URI=https://login.microsoftonline.com/common/oauth2/logout
SESSION_SECRET=
HOSTNAME=

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# GraphQL
API_URL=localhost:8080
HASURA_GRAPHQL_ADMIN_SECRET=<Hasura admin password here>

# Webhook
WEBHOOK_ADDR=localhost:4000